# Copy this file to .env (which is gitignored) and fill in real values.
# .env is loaded by webpack.config.js at build time via DefinePlugin — see the comment there.
OPENROUTER_KEY=sk-or-v1-your-key-here
